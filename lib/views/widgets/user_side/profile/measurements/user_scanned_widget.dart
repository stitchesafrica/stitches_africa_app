import 'package:flutter/material.dart';

class UserScannedWidget extends StatelessWidget {
  const UserScannedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
